<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ToDoList.MainPage"
             BackgroundColor="Grey"
             Title="">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="70"/>
            <RowDefinition/>
            <RowDefinition Height="100"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        
        <Label Style="{StaticResource ToDoList}" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Text="To do List"/>

        <Button Style="{StaticResource AddNewTask}" Grid.Row="1" Grid.Column="0" Text="Add New task" Clicked="AddNewTask"/>
        <Button Style="{StaticResource DeleteTask}" Grid.Row="1" Grid.Column="1" Text="Delete Task" Clicked="DeleteButton_Clicked"/>

        <CollectionView Style="{StaticResource TaskList}" x:Name="tasklist" Grid.Row="2" ItemsSource="{Binding Tasks}" Grid.Column="0" Grid.ColumnSpan="2" SelectionMode="Single">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition/>
                            <RowDefinition/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>
                        
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup Name="CommonStates">
                                <VisualState Name="Normal" />
                                <VisualState Name="Selected">
                                    <VisualState.Setters>
                                        <Setter Property="BackgroundColor" Value="lightgray" />
                                    </VisualState.Setters>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                        
                        <Label Style="{StaticResource LabelTask}" Grid.Row="0" Grid.Column="0" Text="Task Name:"/>
                        <Label Style="{StaticResource LabelStatus}"  Grid.Row="0" Grid.Column="1" Text="Task Status:"/>

                        <Label Style="{StaticResource LabelTaskProgres}" Grid.Row="1" Grid.Column="0" Text="{Binding Name}"/>
                        <CheckBox Style="{StaticResource CheckBox}" Grid.Row="1" Grid.Column="1"/>
                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <Button Style="{StaticResource TaskDetails}" Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" Text="Show task details"/>
    </Grid>

</ContentPage>
